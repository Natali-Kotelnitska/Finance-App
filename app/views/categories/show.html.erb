<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit this category", edit_category_path(@category), class="btn btn-success" %> |
  <%= link_to "Назад до категорій", categories_path, class: "btn btn-secondary"  %>  

  <%= button_to "Destroy this category", @category, method: :delete, class="btn btn-warning" %>
</div>
